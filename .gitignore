__pycache__/
*.py[cod]
.venv/
venv/
.env
.env.local
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Local working data — never commit large source dumps or caches.
var/
data/cache/
data/companies_house/*.zip
data/companies_house/*.csv
data/companies_house/*.sqlite
data/raw/
*.sqlite
*.sqlite3
*.db
reports/local/
reports/runs/
reports/smoke/
reports/outbox/
# Generated sales working files. Every one is reproducible from a command, and
# they contain prospect names and lead data that has no reason to be in git.
reports/previews/
reports/samples/
reports/outreach_drafts/
reports/backups/

# Copy-on-write safety copies of the prospect and suppression lists. The
# tracked files are the record; these are the undo button for the last write.
outreach/backups/
